# E-commerce CRUD API

A **RESTful API** for managing products in an E-commerce platform using **Node.js**, **Express**, and **MongoDB**.  
This project demonstrates **full CRUD operations** with proper project structure and secure environment management.

---

## 🚀 Features

- Create, Read, Update, Delete products
- MongoDB as the database
- Environment variables stored securely in `.env`
- Structured project with controllers, routes, and models
- Secure `.env` handling and `.gitignore` setup
- Ready to extend with authentication, search, filtering, and pagination

---

## 🗂 Project Structure

